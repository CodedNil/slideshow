# Set your preferred output (resolution and refresh rate)
output * resolution 1920x1080@60Hz

# Disable the unnecessary status bar for pure fullscreen
bar {
    status_command swaybar -f
    mode invisible
}

# Hide the cursor when idle
seat * hide_cursor 500

# Start your application in fullscreen mode on startup
exec /home/adept/slideshow --fullscreen

# Optional: Lock keyboard shortcuts for added security in kiosk mode
bindsym --release Mod4+Shift+q exec killall slideshow

# Disable xwayland
xwayland disable
